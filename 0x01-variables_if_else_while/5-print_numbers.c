#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 * main- prints numbers
 * Return: return values
 * Description: this code prints numbers
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
