#include "main.h"

/**
*_islower -  check lower case character.
* @c: character to check a case
*
* Return: Returns 1 if c is lowercase otherwise 1.
*/
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
