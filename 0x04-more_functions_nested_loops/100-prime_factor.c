#include <stdio.h>
#include <math.h>

/**
 * main - distiguish prime numbers.
 *
 * Return: Always 0.
 */

int main(void)
{
	unsigned int long num = 612852475143, i = (int) sqrt(n);
	while (1)
	{
		if (num % a == 0)
		{
			printf("%lu \n", num / i);
			break;
		}
		i--;
	}
	return (0);
}
